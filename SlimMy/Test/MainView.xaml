<Window x:Class="SlimMy.Test.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SlimMy.Test" d:DataContext="{d:DesignInstance Type=local:MainPageViewModel}"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Logged-in User ID:" Margin="10"/>
            <TextBlock Text="{Binding LoggedInUserId}" FontWeight="Bold" Margin="10"/>
        </StackPanel>
    </Grid>
</Window>
