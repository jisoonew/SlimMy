<Window x:Class="SlimMy.View.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SlimMy.ViewModel"
        mc:Ignorable="d"
        Title="Login" Height="350" Width="300">
    <Window.DataContext>
        <local:Login />
    </Window.DataContext>
    <StackPanel Width="299" Height="300">
        <!-- 가로 또는 세로 방향으로 지정할 수 있는 단일 선에 따라 자식 요소를 정렬하는 판넬 -->
        <Label HorizontalAlignment="Center" Margin="0,10,0,10">LOGIN</Label>
        <StackPanel Width="200">
            <Label>User Email</Label>
            <TextBox></TextBox>
            <Label Margin="0,20,0,0">Password</Label>
            <TextBox></TextBox>
            <Button x:Name="login" Width="200" Margin="0,20,0,0" Click="login_Click">로그인</Button>
        </StackPanel>

        <StackPanel Margin="10">
            <TextBox Text="{Binding User.Name, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="0,0,0,10"/>
            <Button Content="Show Name" Command="{Binding ShowNameCommand}" Width="100" />
            <TextBlock Text="{Binding DisplayName}" Margin="0,10,0,0"/>
        </StackPanel>
    </StackPanel>
</Window>
