<Window x:Class="SlimMy.View.ChattingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SlimMy.ViewModel" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors" 
        d:DataContext="{d:DesignInstance Type=local:ChattingWindow}"
        mc:Ignorable="d"
        Title="ChattingWindow" Height="550" Width="550"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="6*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="0" Width="534">
            <Grid HorizontalAlignment="Right" Width="534">
                <!-- 버튼과 팝업 -->
                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Width="60">
                    <Button Width="50" Height="40" Command="{Binding TogglePopupCommand}" HorizontalAlignment="Right">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="Menu" Width="20" Height="20" VerticalAlignment="Stretch"/>
                        </StackPanel>
                    </Button>

                    <!-- Popup -->
                    <Popup IsOpen="{Binding IsPopupOpen}" Placement="Bottom" PlacementTarget="{Binding ElementName=OptionsButton}">
                        <Border Background="White" BorderBrush="Gray" BorderThickness="1" Padding="10">
                            <StackPanel>
                                <!-- 선택지들 -->
                                <Button Content="멤버 내보내기" Command="{Binding Option1Command}" Visibility="{Binding IsHost, Converter={StaticResource BoolToVisibilityConverter}}"  Margin="0,5"/>
                                <Button Content="방장 위임" Command="{Binding Option2Command}" Visibility="{Binding IsHost, Converter={StaticResource BoolToVisibilityConverter}}"  Margin="0,5"/>
                                <Button Content="채팅방 나가기" Command="{Binding Option3Command}" Margin="0,5"/>
                            </StackPanel>
                        </Border>
                    </Popup>
                </StackPanel>
            </Grid>
        </StackPanel>
        
        <ListView x:Name="messageListView" VirtualizingStackPanel.IsVirtualizing="False"
          VirtualizingStackPanel.ScrollUnit="Pixel" Grid.Row="1" Margin="0,0,0,10">
            <ItemsControl x:Name="MessageList" ItemsSource="{Binding MessageList}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock x:Name="MessageSenderText" Text="{Binding Message}" Margin="5" TextAlignment="{Binding Alignment}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ListView>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="Send_Text_Box" Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}" Height="70" />
            <Button Grid.Column="1" x:Name="Send_btn" Margin="10,0,0,0" Content="전송" Command="{Binding SendCommand}" Height="70" />
        </Grid>
    </Grid>
</Window>
